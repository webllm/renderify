{
  "name": "renderify",
  "version": "0.10.0",
  "description": "Official Renderify SDK facade",
  "main": "dist/renderify.cjs",
  "types": "dist/renderify.d.ts",
  "module": "dist/renderify.mjs",
  "exports": {
    ".": {
      "types": "./dist/renderify.d.ts",
      "import": "./dist/renderify.mjs",
      "require": "./dist/renderify.cjs",
      "default": "./dist/renderify.mjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc -p tsconfig.json --pretty false --noEmit",
    "lint": "biome lint src",
    "clean": "node -e \"for (const dir of ['dist', 'dist-types']) require('node:fs').rmSync(dir, { recursive: true, force: true })\"",
    "build:repo": "tsup --config tsup.config.ts",
    "watch:repo": "tsup --config tsup.config.ts --watch"
  },
  "author": "unadlib",
  "license": "MIT",
  "keywords": [
    "renderify",
    "runtime",
    "llm",
    "ui",
    "sdk",
    "typescript"
  ],
  "homepage": "https://github.com/webllm/renderify#readme",
  "bugs": {
    "url": "https://github.com/webllm/renderify/issues"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/webllm/renderify.git",
    "directory": "packages/renderify"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "sideEffects": false,
  "dependencies": {
    "@renderify/core": "workspace:^",
    "@renderify/ir": "workspace:^",
    "@renderify/llm": "workspace:^",
    "@renderify/runtime": "workspace:^",
    "@renderify/security": "workspace:^"
  }
}
