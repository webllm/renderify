# Changesets

This directory stores pending version changes for workspace packages.

Useful commands:

- `pnpm changeset` to create a new changeset.
- `pnpm version-packages` to apply version bumps.
- `pnpm release` to publish to npm.
