---
"@renderify/llm": minor
"@renderify/core": minor
"@renderify/cli": minor
"renderify": minor
---

Add a built-in OpenAI Codex Responses provider with Renderify-managed Codex OAuth login, runtime credential refresh, `gpt-5.5` as the Codex default model, streaming Responses request bodies for the Codex backend, Codex-compatible RuntimePlan structured output, a 300s Codex request default, and playground LLM log redaction for Codex requests.
